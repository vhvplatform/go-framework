# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts should always use LF line endings
*.sh text eol=lf

# Go source files should always use LF line endings
*.go text eol=lf

# Documentation files should always use LF line endings
*.md text eol=lf

# YAML files should always use LF line endings
*.yml text eol=lf
*.yaml text eol=lf

# Makefiles must use LF line endings
Makefile text eol=lf
makefile text eol=lf

# Docker files should always use LF line endings
Dockerfile* text eol=lf
*.dockerfile text eol=lf
